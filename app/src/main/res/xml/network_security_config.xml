<?xml version="1.0" encoding="utf-8"?>
<!--
    Network Security Configuration for Project Myriad
    Enforces HTTPS connections and implements certificate pinning for enhanced security
-->
<network-security-config>
    <!-- Domain-specific configuration with certificate pinning -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="false">api.example.com</domain>
        <!-- Certificate pinning for API endpoints (update with actual certificates) -->
        <pin-set expiration="2025-12-31">
            <!-- Add actual certificate pins when API endpoints are defined -->
            <!-- Example: <pin digest="SHA256">7HIpactkIAq2Y49orFOOQKurWxmmSFZhBCoQYcRhJ3Y=</pin> -->
        </pin-set>
    </domain-config>

    <!-- Debug configuration (only for debug builds) -->
    <debug-overrides>
        <trust-anchors>
            <!-- Trust user-added CAs for debugging HTTPS traffic -->
            <certificates src="user"/>
            <certificates src="system"/>
        </trust-anchors>
    </debug-overrides>

    <!-- Base configuration for all other domains -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system"/>
        </trust-anchors>
    </base-config>
</network-security-config>