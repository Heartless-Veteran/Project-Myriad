# Keystore Configuration Example
# Copy this file to keystore.properties and fill in your actual values
# NEVER commit the actual keystore.properties file to version control

# Path to your keystore file (relative to app module)
storeFile=myriad-release-key.jks

# Keystore password
storePassword=YOUR_KEYSTORE_PASSWORD_HERE

# Key alias
keyAlias=myriad-key-alias

# Key password
keyPassword=YOUR_KEY_PASSWORD_HERE

# Instructions:
# 1. Generate a keystore file:
#    keytool -genkey -v -keystore myriad-release-key.jks -keyalg RSA -keysize 2048 -validity 10000 -alias myriad-key-alias
# 2. Place the keystore file in the app/ directory
# 3. Copy this file to keystore.properties and fill in your actual passwords
# 4. The keystore file and keystore.properties are automatically excluded from git